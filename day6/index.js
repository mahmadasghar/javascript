// Forgot to study assignment operators 

// Assignment Operators:
    // These operators assign some value to a variable
    // - = (Equal To)
    // - -= (subtract & equal to)
    // - += (add & equal to)
    // - *= (multiply & equal to)
    // - /= (divide & equal to)
    // - &= (if both values is true then assign)
    // - |= (if either of the values in true then assign)

let aNumber = 12
let abc = 56

aNumber = aNumber + 2
// below statement is the shortcut for above statement
aNumber += 5

console.log('aNumber: ', aNumber)


